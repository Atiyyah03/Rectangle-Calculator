
def area(length, width): #Funtion to calculate the area of the rectangle
  return length*width
  
def perimeter(length, width): #Funtion to calculate the perimeter of the rectangle
  return 2*(length + width)